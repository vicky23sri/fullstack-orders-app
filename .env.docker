# Database Configuration
POSTGRES_DB=orders_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# Use the Docker service name 'postgres' instead of 'localhost'
DATABASE_URL="postgresql://postgres:postgres123@postgres:5432/orders_db?schema=public"

# Application Configuration
NODE_ENV=production
PORT=3001

# Redis Configuration
REDIS_URL=redis://redis:6379

# API Configuration (this should remain localhost for browser access)
NEXT_PUBLIC_API_URL=http://localhost:3001